# Project brief — contains client context, not for public repo
CONTEXT.md

# Data — never commit client data
data/
!data/raw/.gitkeep
!data/processed/.gitkeep

# figures/ — PNGs are tracked so they are browsable on GitHub
# (only the .gitkeep placeholder was tracked before; now all PNGs are included)

# Raw data formats
*.csv
*.parquet

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Virtual environments
venv/
.venv/

# Word intermediates — only the PDF is committed
*.docx
